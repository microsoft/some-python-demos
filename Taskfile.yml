version: '3'

tasks:
  maf-demo:
    dir: ./maf
    cmds:
      - uv run --frozen maf
  fastapi-demo:
    dir: ./fastapi
    cmds:
      - uv run --frozen fastapi-demo
  frontend:
    dir: ./frontend
    cmds:
      - npm run dev
  demo:
    deps: [fastapi-demo, maf-demo, frontend]
